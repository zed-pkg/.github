tmp
temp
tmp/worktrees
temp/worktrees
/blueprints/zed-eclipse/target/
__pycache__/
*.py[cod]

# BEGIN ores-sops dotenv policy
# Plaintext dotenv is local-only at every depth, including common suffix variants.
*.env
*/*.env
*/**/*.env
.env.*
*.env.*
!.env.example

# Decrypted material is never tracked.
/env/dec/

# Only ciphertext matching the organization-wide encrypted-profile contract is trackable.
/env/enc/*
!/env/enc/*.env.enc
# END ores-sops dotenv policy
